// controls stage

class StageController {

	public:
		StageController() {

		}
	protected:
		// foreground, characters, background, maybe more layers. how to do that idk
		// unless its all one layer and i just enum which is first? or a stack? need to think

};