#pragma once

// declare sprite class

class Sprite {
public:
	// renderVisual(Window * win, ??? asset); -- idk what type the asset should be
	// getLocation();
	// setLocation(int x, int y);
	// getObjDims();
	// setObjDims(int len, int wid);
};