// handles game stuff so main doesn't have to
#include "scene.h"

class Scene {
	// this is just a massive list of all the scenes

	// a scene contains music, a backdrop, typically player interactions, objects in varying grounds (foreground, background, etc), typically player character
	// fight scenes also contain all of the above, but sometimes more in terms of a menu, which i think is also in regular walking scenes

	int loadTitleScene(Window* window) {
		// music handler
		// backdrop controller (always static)
		// player controller (interactions, movement, sprite)
		// object controller (objects)
		// probably a prompt controller?
		// probably a dialogue box controller?
	}
};

